# Database Relational Models

This directory defines the SQLAlchemy ORM models representing our relational PostgreSQL schemas.

## Purpose
- Map python classes to database tables (e.g. `Ticket`, `Team`, `User`).
- Establish strict relationship back-references and lazy loading attributes.
- Maintain consistency with the data dictionary defined under `docs/database/`.
