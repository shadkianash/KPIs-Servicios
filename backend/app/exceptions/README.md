# Exceptions Package

This package manages the custom exception modeling and handlers.

## Purpose
- Define custom application domain error exceptions.
- Register global exception interceptors mapping standard Pydantic and custom exceptions to a unified JSON layout.
