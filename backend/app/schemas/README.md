# Validation Schemas

This folder contains Pydantic v2 schemas used for strict validation of inputs and outputs.

## Purpose
- Define structural validation classes (e.g. `TicketCreate`, `SlaResponse`).
- Provide automatic OpenAPI/Swagger typing serialization.
- Validate, sanitize, and convert incoming payload types securely.
