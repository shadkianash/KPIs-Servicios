# Core System Configuration

This directory contains core configuration profiles, runtime settings, and security credentials.

## Purpose
- Load and parse environments variables using Pydantic Settings.
- Manage CORS headers, log formatting, and custom error types.
- Export global security middleware configurations (e.g. JWT handling, hashing).
