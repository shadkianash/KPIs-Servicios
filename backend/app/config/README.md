# Configuration Package

This package manages the centralized Pydantic Settings and environments loading configurations.

## Contents
- `settings.py`: The settings configuration class (using `pydantic-settings`), validated from environmental bindings, cached as a singleton.
